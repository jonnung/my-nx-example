# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build my-nx-example`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t my-nx-example`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system my-nx-example && \
          adduser --system -G my-nx-example my-nx-example

COPY dist/apps/my-nx-example my-nx-example/
RUN chown -R my-nx-example:my-nx-example .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix my-nx-example --omit=dev -f install

CMD [ "node", "my-nx-example" ]
